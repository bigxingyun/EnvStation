# 行尾统一：仓库与工作区都用 LF。
#
# 为什么不沿用 git 默认的"仓库存 LF、Windows 检出成 CRLF"：
# 本仓库有测试把 TOML 题面写成 C# 原始字符串字面量，源文件一旦被检出成 CRLF，
# 题面里的换行就变成 \r\n，于是同一份代码、同一份测试在不同检出设置下结果不同
# （SC-05 就这么挂过）。解析器侧也已按 TOML 规范把多行字符串的换行归一为 \n，
# 但让工作区保持 LF 能从源头消掉这类差异。
#
# 必须显式写 eol=lf：本机 core.autocrlf=true 时只写 text=auto 会被它覆盖，
# 结果是"仓库里是 LF、检出却是 CRLF"——正是上面那个坑。
* text=auto eol=lf

# 二进制：不做任何行尾或编码转换
*.png binary
*.ico binary
*.jpg binary
*.zip binary
*.gz binary
*.dll binary
*.exe binary
*.pdb binary
